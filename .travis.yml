install:
  - bash -ex .travis-ocaml.sh
  - opam install -y oasis ocamlfind
  - echo $PATH
  - eval `opam config env`
sudo: required
script: echo $PATH; eval `opam config env`;echo $PATH; make
env:
  - OCAML_VERSION=4.04
addons:
  apt:
    packages:
      - ocaml-findlib
