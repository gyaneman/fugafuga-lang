before_install:
  - sudo apt-get update
  - sudo apt-get install -y ocaml-findlib
install: "opam install -y oasis; bash -ex .travis-ocaml.sh"
sudo: required
script: make
env:
  - OCAML_VERSION=4.04
    #addons:
    #apt:
    #packages:
    #  - ocaml-findlib
